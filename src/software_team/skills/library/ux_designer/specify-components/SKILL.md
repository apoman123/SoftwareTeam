---
name: specify-components
description: Use when handing a design to engineering, to specify each component's states, validation rules and user-facing copy.
---

# Specify components and states

For each interactive component, define its full set of states:

- **default, hover/focus, loading, empty, error, success.**
- Validation rules and the exact **copy** for empty and error states.

Keep designs technically feasible — confirm effects are implementable by the front-end before committing.
