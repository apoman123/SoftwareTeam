---
name: run-tests
description: Runs the test suite locally and self-checks the result. Use before declaring work done.
tool: run_tests
---

# Run the tests

Run the project's test suite locally (with the stack's standard test command) before
declaring work done.

Green tests are the **definition of done** for an implementation task. If anything is red,
fix it before handing off.
